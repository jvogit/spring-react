import { React } from "react";
import SignupForm from "components/forms/SignupForm";

export const Login = () => {
  return (
    <section>
      <SignupForm />
    </section>
  );
};

export default Login;